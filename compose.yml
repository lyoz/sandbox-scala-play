services:
  mysql:
    image: mysql:8.0.31
    command: mysqld --character-set-server=utf8mb4 --collation-server=utf8mb4_bin
    environment:
      MYSQL_DATABASE: lyoz
      MYSQL_ROOT_PASSWORD: pw
    ports:
      - 53306:3306
    volumes:
      - mysql:/var/lib/mysql

volumes:
  mysql:
